spring:
  datasource:
    url: jdbc:h2:mem:mis_agent_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never

ai:
  provider: mock
  time-zone: Asia/Shanghai

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
