<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sdjzuxg.collegemanagesystem.mapper.FinalExamTeacherMapper">

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO FINAL_EXAM_TEACHER (exam_id, teacher_id, invigilate_role)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.exam.examId}, #{item.teacher.teacherId}, #{item.invigilateRole})
        </foreach>
    </insert>

    <select id="countExistByExamAndTeacher" resultType="int">
        SELECT COUNT(*) FROM FINAL_EXAM_TEACHER
        WHERE exam_id = #{examId} AND teacher_id = #{teacherId} AND invigilate_role = #{invigilateRole}
    </select>

    <delete id="deleteByExamIds">
        DELETE FROM FINAL_EXAM_TEACHER WHERE exam_id IN
        <foreach collection="examIds" item="examId" open="(" separator="," close=")">
            #{examId}
        </foreach>
    </delete>
</mapper>
