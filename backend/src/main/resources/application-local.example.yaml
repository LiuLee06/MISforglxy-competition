# 本地开发配置示例
# 使用时请复制到 backend/application-local.yaml，并替换为实际值。
# 该文件通过启动参数 additional-location 加载。

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/你的数据库名?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&useServerPrepStmts=true
    username: glxyxg
    password: glxy

ai:
  provider: mock
  agent:
    enabled: true
  # 如果使用 DeepSeek，请改为 deepseek，并填写 API Key
  # provider: deepseek
  # deepseek:
  #   api-key: 你的DeepSeek_API_Key
  #   model: deepseek-v4-flash